#include <bits/stdc++.h>

std::vector <std::string> swap_str(int reference_no, std::vector<std::string> permutations, std::string s1){
    for(int i=0;i<s1.length(); i++){
        
    }
    
}

int main(void){
    std::string s1, s2;
    std::vector <std::string> permutations; 
    std::cin >> s1 >> s2;
    int reference_no=0;

    swap_str(reference_no, permutations, s1);

    for(int i=0;i<permutations.size(); i++){
        std::cout << permutations[i];
    }

}