#include <bits/stdc++.h>

int binary_src()

int main(void){
    int soldiers=0, b_power=0, rounds=0;
    std::cin >> soldiers;
    int s_power[soldiers];

    for(int i=0;i<soldiers;i++)
        std::cin >> s_power[i];

    std::cin >> rounds;

    for(int i=0;i<rounds;++){
        std::cin >> b_power;

    }
    
}